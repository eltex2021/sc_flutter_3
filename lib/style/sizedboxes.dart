import 'package:flutter/material.dart';

class SizeBox12 extends StatelessWidget {
  const SizeBox12({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 12,
    );
  }
}

class SizeBox24 extends StatelessWidget {
  const SizeBox24({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 24,
    );
  }
}

class SizeBox16 extends StatelessWidget {
  const SizeBox16({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 16,
    );
  }
}